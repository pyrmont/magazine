<!doctype html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta http-equiv='X-UA-Compatible' content='IE=edge;chrome=1' />
        
        <title>inqk Magazine</title>
        
        <%= stylesheet_link_tag "reset" %>
        <link href='http://fonts.googleapis.com/css?family=Sue+Ellen+Francisco|Oswald|Crimson+Text:400,400italic,600,600italic,700,700italic' rel='stylesheet' type='text/css'>
        <%= stylesheet_link_tag "base" %>        
    </head>
    <body>

        <header>
            <p id="colophon">
                Inqk is a magazine written by Michael Camilleri every so often.
            </p>
            <nav class='<%= ' categorised' if categorised || current_page.data.category %>'>
                <ul>
                    <%
                    if category == 'Culture' || current_page.data.category == 'Culture'
                        culture_class = 'highlight'
                    elsif category == 'Personal' || current_page.data.category == 'Personal'
                        personal_class = 'highlight'
                    elsif category == 'Society' || current_page.data.category == 'Society'
                        society_class = 'highlight'
                    elsif category == 'Technology' || current_page.data.category == 'Technology'
                        technology_class = 'highlight'
                    end
                    %>
                    <li id="home"><%= link_to 'Inqk', '/' %></li>
                    <li id="culture" class="<%= culture_class %>"><%= link_to 'Culture', '/culture/' %></li>
                    <li id="personal" class="<%= personal_class %>"><%= link_to 'Personal', '/personal/' %></li>
                    <li id="society" class="<%= society_class %>"><%= link_to 'Society', '/society/' %></li>
                    <li id="technology" class="<%= technology_class %>"><%= link_to 'Technology', '/technology/' %></li>
                </ul>
            </nav>
        </header>
        
        <section class="content<%= ' categorised' if categorised %>">
            
            <%= yield %>
            
        </section>
        
        <footer>
            
            <nav>
                <ul>
                    <li><%= link_to 'About This Project', '/about.html' %></li>
                </ul>
            </nav>
            
            <p>Original work placed in the public domain. All rights are disclaimed.</p>
            
        </footer>

    </body>
</html>
